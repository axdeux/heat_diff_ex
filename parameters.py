


base_graph_temp = 1
hotspot_temp = 100

min_gif_cmap_temp = base_graph_temp
max_gif_cmap_temp = 10#hotspot_temp

dt = 0.5
alpha = 0.5 # weird bug if alpha or dt is > 0.5

total_time = 500

#Options are 'candle', 'coffee', 'office' and 'abstract'
image_name = 'abstract'

#Available for 'candle', 'coffee' and 'office'
heatmap = False


#Optional for continuous heat source, only if heatmap is True
continous_heat = False

